const numero1 = 20;
const numero2 = "20";

console.log(parseInt(numero2)); // Esto es uan funcion, usamos una funcion para convertir de  string a numero

console.log(numero1.toString()); // Funcion para convertir de numero a  string


// A continuacion llamaremos una funcion c

function sumar() {
      console.log(2 + 2);
}

sumar();